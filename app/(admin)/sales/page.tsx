import { today } from '@/lib/utils';
import ActionForm from '@/components/ActionForm';
import db from '@/lib/db';import PageHead from '@/components/PageHead';import StatusBadge from '@/components/StatusBadge';import { addSale,deleteSale,recordPayment } from '@/app/actions';import { money,shortDate } from '@/lib/utils';
export default async function Sales(){const clients=(await db.prepare('SELECT id,name FROM clients ORDER BY name').all()) as any[];const sales=(await db.prepare(`SELECT s.*,c.name client_name FROM sales s JOIN clients c ON c.id=s.client_id ORDER BY s.id DESC`).all()) as any[];const total=sales.reduce((a,x)=>a+Number(x.final_amount||0),0);const paid=sales.reduce((a,x)=>a+Number(x.paid_amount||0),0);return <><PageHead title="Sales" subtitle="Record client sales, payments and dues."/><div className="kpis"><div className="card kpi"><div className="kpi-top"><span>Total Sales</span></div><div className="kpi-value">{money(total)}</div></div><div className="card kpi"><div className="kpi-top"><span>Total Paid</span></div><div className="kpi-value">{money(paid)}</div></div><div className="card kpi"><div className="kpi-top"><span>Total Due</span></div><div className="kpi-value">{money(total-paid)}</div></div><div className="card kpi"><div className="kpi-top"><span>Transactions</span></div><div className="kpi-value">{sales.length}</div></div></div><section className="card card-pad" style={{marginBottom:18}}><h3 className="section-title" style={{marginBottom:15}}>Add Sale</h3><ActionForm action={addSale}><div className="form-grid"><div className="field"><label htmlFor="client_id">Client</label><select className="select" id="client_id" name="client_id" required><option value="">Select client</option>{clients.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></div><div className="field"><label htmlFor="service">Service</label><input className="input" id="service" name="service" required/></div><div className="field"><label htmlFor="invoice_number">Invoice Number</label><input className="input" id="invoice_number" name="invoice_number"/></div><div className="field"><label htmlFor="sale_date">Sale Date</label><input className="input" type="date" id="sale_date" name="sale_date" defaultValue={today()} required/></div><div className="field"><label htmlFor="amount">Amount</label><input className="input" type="number" step="0.01" id="amount" name="amount" min="0" required/></div><div className="field"><label htmlFor="discount">Discount</label><input className="input" type="number" step="0.01" id="discount" name="discount" min="0" defaultValue="0"/></div><div className="field"><label htmlFor="paid_amount">Paid Amount</label><input className="input" type="number" step="0.01" id="paid_amount" name="paid_amount" min="0" defaultValue="0"/></div><div className="field"><label htmlFor="payment_method">Payment Method</label><select className="select" id="payment_method" name="payment_method"><option>Bank</option><option>Cash</option><option>Bkash</option><option>Nagad</option><option>Card</option><option>Other</option></select></div><div className="field span2"><label htmlFor="notes">Notes</label><textarea className="textarea" id="notes" name="notes"/></div></div><button className="btn btn-primary" style={{marginTop:14}}>Add Sale</button></ActionForm></section><section className="card"><div className="table-wrap"><table className="table"><thead><tr><th>Invoice</th><th>Client</th><th>Service</th><th>Sale</th><th>Paid</th><th>Due</th><th>Status</th><th>Date</th><th></th></tr></thead><tbody>{sales.map(s=><tr key={s.id}><td>{s.invoice_number||'—'}</td><td className="name">{s.client_name}</td><td>{s.service}</td><td>{money(s.final_amount)}</td><td>{money(s.paid_amount)}</td><td>{money(s.final_amount-s.paid_amount)}</td><td><StatusBadge value={s.payment_status}/></td><td>{shortDate(s.sale_date)}</td><td>{s.final_amount > s.paid_amount && <details className="payment-popover"><summary className="btn btn-sm btn-success">Record payment</summary><ActionForm action={recordPayment.bind(null,s.id)}><label className="small" htmlFor={'payment-'+s.id}>Amount received (BDT)</label><input id={'payment-'+s.id} className="input" name="payment" type="number" step="0.01" min="0.01" max={s.final_amount-s.paid_amount} required /><button className="btn btn-primary btn-sm">Save payment</button></ActionForm></details>}<ActionForm confirmMessage="Delete this record? This cannot be undone. Deleting a client also removes their follow-ups and sales." action={deleteSale.bind(null,s.id,s.client_id)}><button className="btn btn-sm btn-danger">Delete</button></ActionForm></td></tr>)}{!sales.length&&<tr><td colSpan={9}><div className="empty">No sales yet.</div></td></tr>}</tbody></table></div></section></>}
