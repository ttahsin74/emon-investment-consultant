import ActionForm from '@/components/ActionForm';
import PageHead from '@/components/PageHead';
import StatusBadge from '@/components/StatusBadge';
import db from '@/lib/db';
import { today, shortDate } from '@/lib/utils';
import { deleteAttendance, saveAttendance } from '@/app/actions';
export default async function Attendance(){const rows=(await db.prepare('SELECT * FROM attendance ORDER BY attendance_date DESC LIMIT 100').all()) as any[];return <><PageHead title="Manual Attendance" subtitle="Add or correct attendance by date; saving the same date updates the existing record."/><section className="card card-pad form-card"><h3 className="section-title">Attendance Entry</h3><ActionForm action={saveAttendance} style={{marginTop:16}}><div className="form-grid"><div className="field"><label>Date *</label><input className="input" type="date" name="attendance_date" defaultValue={today()} required/></div><div className="field"><label>Status</label><select className="select" name="status"><option>Present</option><option>Absent</option><option>Late</option><option>Leave</option><option>Holiday</option></select></div><div className="field"><label>Check In</label><input className="input" type="time" name="check_in"/></div><div className="field"><label>Check Out</label><input className="input" type="time" name="check_out"/></div><div className="field span2"><label>Remarks</label><input className="input" name="remarks"/></div></div><div className="form-actions"><button className="btn btn-primary">Save Attendance</button></div></ActionForm></section><section className="card" style={{marginTop:18}}><div className="table-wrap"><table className="table"><thead><tr><th>Date</th><th>Check In</th><th>Check Out</th><th>Status</th><th>Remarks</th><th></th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{shortDate(r.attendance_date)}</td><td>{r.check_in||'—'}</td><td>{r.check_out||'—'}</td><td><StatusBadge value={r.status}/></td><td>{r.remarks||'—'}</td><td><ActionForm confirmMessage="Delete this attendance record?" action={deleteAttendance.bind(null,r.id)}><button className="btn btn-sm btn-danger">Delete</button></ActionForm></td></tr>)}{!rows.length&&<tr><td colSpan={6}><div className="empty">No attendance records.</div></td></tr>}</tbody></table></div></section></>}
