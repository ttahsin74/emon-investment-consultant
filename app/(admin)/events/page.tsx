import ActionForm from '@/components/ActionForm';
import PageHead from '@/components/PageHead';
import StatusBadge from '@/components/StatusBadge';
import db from '@/lib/db';
import { today, shortDate } from '@/lib/utils';
import { addEventGuest, deleteEventGuest, updateEventGuestStatus } from '@/app/actions';
export default async function Events(){const rows=(await db.prepare('SELECT * FROM event_guests ORDER BY event_date DESC,id DESC').all()) as any[];return <><PageHead title="Event Guests" subtitle="Register event guests and track invitation or attendance status."/><section className="card card-pad form-card"><h3 className="section-title">Add Event Guest</h3><ActionForm action={addEventGuest} style={{marginTop:16}}><div className="form-grid"><div className="field"><label>Name *</label><input className="input" name="name" required/></div><div className="field"><label>Phone</label><input className="input" name="phone"/></div><div className="field"><label>Email</label><input className="input" type="email" name="email"/></div><div className="field"><label>Guest Type</label><select className="select" name="guest_type"><option>Guest</option><option>VIP</option><option>Client</option><option>Partner</option></select></div><div className="field"><label>Event Name *</label><input className="input" name="event_name" required/></div><div className="field"><label>Event Date *</label><input className="input" type="date" name="event_date" defaultValue={today()} required/></div><div className="field"><label>Status</label><select className="select" name="status"><option>Invited</option><option>Confirmed</option><option>Attended</option><option>Declined</option></select></div><div className="field span2"><label>Remarks</label><textarea className="textarea" name="remarks"/></div></div><div className="form-actions"><button className="btn btn-primary">Add Guest</button></div></ActionForm></section><section className="card" style={{marginTop:18}}><div className="table-wrap"><table className="table"><thead><tr><th>Guest</th><th>Event</th><th>Date</th><th>Type</th><th>Status</th><th>Action</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><b>{r.name}</b><div className="small muted">{r.phone||r.email||'—'}</div></td><td>{r.event_name}</td><td>{shortDate(r.event_date)}</td><td>{r.guest_type}</td><td><StatusBadge value={r.status}/></td><td><ActionForm action={updateEventGuestStatus.bind(null,r.id)}><div className="actions"><select className="select" name="status" defaultValue={r.status}><option>Invited</option><option>Confirmed</option><option>Attended</option><option>Declined</option></select><button className="btn btn-sm btn-success">Save</button></div></ActionForm><ActionForm confirmMessage="Delete this guest?" action={deleteEventGuest.bind(null,r.id)}><button className="btn btn-sm btn-danger" style={{marginTop:6}}>Delete</button></ActionForm></td></tr>)}{!rows.length&&<tr><td colSpan={6}><div className="empty">No event guests yet.</div></td></tr>}</tbody></table></div></section></>}
